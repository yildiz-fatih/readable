# Ports
API_PORT=8080
READABILITY_SERVICE_PORT=8081
EPUB_SERVICE_PORT=8082

# Service URLs
READABILITY_SERVICE_URL=http://readability-service:8081
EPUB_SERVICE_URL=http://epub-service:8082
GOTENBERG_URL=http://gotenberg:3000

# Database (Postgres)
POSTGRES_USER=readableuser
POSTGRES_PASSWORD=readablepassword
POSTGRES_DB=readabledb
POSTGRES_URL=postgres://readableuser:readablepassword@postgres:5432/readabledb?sslmode=disable # keep in sync with the three above

# Object storage (SeaweedFS / S3)
S3_INTERNAL_URL=http://seaweedfs:8333
S3_PUBLIC_URL=http://localhost:8333
S3_BUCKET=readable
AWS_ACCESS_KEY_ID=changeme
AWS_SECRET_ACCESS_KEY=changeme
